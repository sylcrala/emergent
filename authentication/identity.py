import os
import getpass
from logs._logger import logger, USER_LEVEL



"""
def get_authenticated_user():
    user = getuser()
    logger.USER_LEVEL(f"[Iris] Detected user: {user}")

    key = os.environ.get("IRIS_KEY")
    if not key:
        key = getpass(f"Enter decryption key for {user}: ")

    return user, key
"""
